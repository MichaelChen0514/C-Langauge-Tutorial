#include <stdio.h>
#include <stdlib.h>
int reset()
{
    return 0;
}
int plus1 (int a)
{
    return a+1;
}
int Negativenumber (int a)
{
return -a;
}
int multi2(int a)
{
    return 2*a;
}
int exit1(void)
{
    return 0;
}
int main()
{
    int a;
    do
        printf("0.restore the initial value (n=1)\n");
        printf("1.negate the number (+/-)\n");
        printf("2.Multiple by 2\n");
        printf("3.exit\n");
        scanf("%d", 0, 1, 2, 3,9);
        a=(%d,0, 1, 2, 3, 9);
        int t;
        if a=0
            scanf ("%d", &t);
            printf("%d", t\n);
        else if a=1
            printf("%d",Negativenumber(t)")

        else if a=2
            printf("%d", multi2(t));

        else if a=3
            printf("%d", plus1(t);
        else
            reset (t);

        while (a!=9);

    return 0;
}
