#include <stdio.h>
#include <stdlib.h>

int main()
{
    int row, col,n;
    scanf("%d", &n);
    int x;
    int y=1;
    for (row=1; row<=y;row++)
    {
        for
        printf("O");
            while(col<=n)
                col=1;
                col++;
            printf(" ");
    }
    return 0;
}
